<form action="{{ url('admin_customer_delete', { id: customer.id }) }}" method="post" data-confirmation="true" id="delete-form" style="display: inline-block;">
  <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
  <button type="submit" class="btn btn-link">{{ 'action.delete_customer'|trans }}</button>
</form>
