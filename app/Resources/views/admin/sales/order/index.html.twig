{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'title.orders'|trans }} / {{ parent() }}{% endblock %}
{% block body_id %}admin_sales_order_index{% endblock %}

{% block body %}
    <div class="row wrapper border-bottom white-bg page-heading">
        <div class="col-lg-10">
            <h2>{{ 'title.orders'|trans }}</h2>
            <ol class="breadcrumb">
                <li><a href="{{ path('admin_index') }}">{{ 'title.home'|trans }}</a></li>
                <li class="active"><strong>{{ 'title.orders'|trans }}</strong></li>
            </ol>
        </div>
        <div class="col-lg-2"></div>
    </div>

    <div class="wrapper wrapper-content animated fadeInRight">
        <div class="row">
            <div class="col-lg-12">
                <a href="{{ path('admin_sales_order_new') }}" class="btn btn-success">{{ 'action.create_new_order'|trans }}</a>

                <div class="ibox">
                    <div class="ibox-content">
                        <table class="table table-striped">
                            <thead>
                            <tr>
                                <th>{{ 'label.id'|trans }}</th>
                                <th class="text-right">{{ 'label.action'|trans }}</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for order in pagination %}
                                <tr>
                                    <td>{{ order.id }}</td>
                                    <td class="text-right"><a href="{{ path('admin_sales_order_edit', { id: order.id }) }}" class="btn-white btn btn-xs">{{ 'action.edit'|trans }}</a></td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="2">{{ 'order.no_orders_found'|trans }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                            <tfoot>{{ knp_pagination_render(pagination) }}</tfoot>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
